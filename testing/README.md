# Testing

Hardware-independent fixtures and fakes belong here.

The first production observer scope should bring forward the proven fake-sensor pattern: framework APIs behind a repository seam, Android-light segment/spool logic tested on the host JVM, and physical-device tests only where hardware behavior is the point.

